package com.horizonloop.app.core.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark Navy + Cyan palette ──────────────────────────────────────────────────
// Deep navy background with a bright cyan accent. Clean, modern, professional.
// This is the ONE color combination used across the entire app.

// Primary surfaces
val Deep = Color(0xFF0B1220)      // deep navy — main background
val Surface = Color(0xFF111A2E)   // dark blue — cards, elevated surfaces
val Muted = Color(0xFF1A2440)     // slightly lighter navy — subtle backgrounds
val Mid = Color(0xFF94A3B8)       // cool slate gray — muted text & icons
val Dark = Color(0xFFF1F5F9)      // off-white — primary text

// Bright cyan accent
val Accent = Color(0xFF22D3EE)    // cyan-400 — primary highlight
val AccentSoft = Color(0xFF67E8F9) // cyan-300 — secondary highlight

// Translucent white overlays (for tints, search bar, etc.)
val White15 = Color(0x26FFFFFF)
val White6 = Color(0x0FFFFFFF)
val White8 = Color(0x14FFFFFF)
val White12 = Color(0x1FFFFFFF)
val White20 = Color(0x33FFFFFF)
